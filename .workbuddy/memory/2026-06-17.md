## 2026-06-17 队伍管理模块重构

### 需求
- 队伍管理页面移除赛季和胜负数据显示
- 队伍编辑支持 Logo 文件上传 + 战队海报上传
- 队伍配置 6 个成员分路：上单、中单、射手、游走、打野、替补
- 所属赛季字段改为可选（创建时不必选赛季）

### 改动文件 (9个)
1. `backend/prisma/schema.prisma` — Team 模型新增 `posterUrl String?`
2. `backend/src/types/index.ts` — 新增 TeamMemberPositions 接口，DTO 更新
3. `backend/src/controllers/teams.controller.ts` — seasonId 改为可选
4. `backend/src/services/teams.service.ts` — posterUrl 支持，seasonId 可选
5. `frontend/src/types/index.ts` — Team 接口新增 posterUrl，seasonId 改可空
6. `frontend/src/api/admin.ts` — API 签名更新
7. `frontend/src/pages/admin/TeamManage.tsx` — 表格列重构 + 工具函数
8. `frontend/src/components/admin/TeamFormDialog.tsx` — 完全重写
9. 数据库迁移 `20260618015759_add_team_poster` 已应用

### 结果
- 工程师 IS_PASS: YES，全部编译通过
- QA 8/8 需求通过，1 个低严重度问题（编辑模式赛季选择器静默丢弃）
- 已修复：编辑模式下隐藏赛季选择器
- 前端 vite build 零错误，后端无新增 TS 错误

### 数据模型
- members 字段存储 6 分路 JSON: `{"topLaner":"选手","midLaner":"选手",...}`
- TeamMemberPositions 类型定义在前后端 types 中
- posterUrl 独立字段存储海报图片路径
